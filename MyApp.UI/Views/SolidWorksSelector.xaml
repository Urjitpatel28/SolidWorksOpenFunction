<UserControl x:Class="MyApp.UI.Views.SolidWorksSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="200">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Background="Black" Height="50" Width="auto">
            <Image Source="/Assets/icons8-solidworks-48.png" Width="32" Height="32" Margin="5"/>
            <ComboBox Width="250" Height="35" Background="Black" Foreground="White" Margin="5"
                      SelectedItem="{Binding SelectedVersion, Mode=TwoWay}"
                      ItemsSource="{Binding SolidWorksVersions}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/icons8-solidworks-48.png" Width="20" Height="20" Margin="5"/>
                            <TextBlock Text="{Binding Display}" VerticalAlignment="Center" Foreground="Black"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>
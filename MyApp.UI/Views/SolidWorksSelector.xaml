<UserControl x:Class="MyApp.UI.Views.SolidWorksSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="420">
    <Border Background="{DynamicResource Brush.Surface}"
            CornerRadius="6"
            Padding="12"
            BorderBrush="{DynamicResource Brush.Border}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center">
                <Image Source="/Assets/icons8-solidworks-48.png" Width="32" Height="32" Margin="0,0,8,0"/>
                <TextBlock Text="Select SOLIDWORKS Version"
                           Style="{StaticResource Text.Headline}"/>
            </StackPanel>

            <DockPanel Grid.Row="1" Margin="0,10,0,0">
                <ComboBox Width="260"
                          Margin="0,0,10,0"
                          ItemsSource="{Binding SolidWorksVersions}"
                          SelectedItem="{Binding SelectedVersion, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Image Source="/Assets/icons8-solidworks-48.png" Width="18" Height="18" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding Display}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Button Content="Open"
                        Width="100"
                        Background="{DynamicResource Brush.Primary}"
                        Foreground="White"
                        Padding="8,4"
                        Cursor="Hand"
                        Click="OnOpenClick"/>
            </DockPanel>
        </Grid>
    </Border>
</UserControl>